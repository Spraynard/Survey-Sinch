import React from "react";
import { SurveyComponentItem } from "../types";


const SurveyorCheckbox = ({ label, value }: SurveyComponentItem) =>
    <input type="checkbox" value={value}>label</input>
export const SurveyorCheckboxGroup = ({ items }) => {

}